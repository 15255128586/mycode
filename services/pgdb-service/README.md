# PGDB Service

PostgreSQL service for project-service persistence.

## Docker (dev)

```bash
cd services/pgdb-service
docker compose up -d
```

Database runs at `localhost:5433`.

## Default credentials

- user: `project`
- password: `project`
- database: `project`
